language: node_js
node_js:
    - 0.08
    - 0.10
    - 0.11
notifications:
    irc:
        channels:
            - "chat.freenode.net#tests-always-included"
        skip_join: true
        template:
            - "%{repository_slug}#%{build_number} (%{branch} - %{commit} : %{author}): %{message}"
        use_notice: true
